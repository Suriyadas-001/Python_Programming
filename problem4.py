# PRAKASH MAITI

# 4. Given numbers = [1, 2, 3, 4, 5], use map() with a lambda to cube all numbers.

numbers = [1, 2, 3, 4, 5]
cubed_numbers = list(map(lambda x: x**3, numbers))

print(cubed_numbers)   # <-- This line shows the output